Run the full test suite.

Command:
- pytest -q

Rules:
- do NOT modify code
- do NOT modify tests
- do NOT suggest fixes yet

Output:
- overall test summary
- list of failing tests (if any)
- brief indication of which subsystem each failure belongs to
  (e.g. dynamics, connectivity, presets, RNG)

If all tests pass:
- state clearly that the codebase is consistent
